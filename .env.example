# ============================================================
# Environment Variables for Nurse Appointment System
# ============================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# ============================================================

# ==================== SUPABASE ====================
# Get these from your Supabase project dashboard > Settings > API

SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ==================== ELEVENLABS ====================
# Get these from ElevenLabs dashboard

# Your ElevenLabs API key
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# The Agent ID for your outbound calling agent
# Create this in ElevenLabs Conversational AI dashboard
ELEVENLABS_AGENT_ID=your-agent-id

# ==================== SENDGRID ====================
# Get these from SendGrid dashboard (https://app.sendgrid.com)

# Your SendGrid API key
# Create at: Settings > API Keys
SENDGRID_API_KEY=your-sendgrid-api-key

# Verified sender email address
# Must be verified in SendGrid: Settings > Sender Authentication
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
SENDGRID_FROM_NAME=Your Clinic Name

# Optional: Reply-to email if different from sender
SENDGRID_REPLY_TO_EMAIL=support@yourdomain.com

# ==================== GOOGLE CALENDAR ====================
# Option 1: OAuth2 Credentials (for user-delegated access)
# Create OAuth credentials in Google Cloud Console

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REFRESH_TOKEN=your-refresh-token

# Option 2: Service Account (recommended for server-to-server)
# Create a service account in Google Cloud Console and download JSON key

GOOGLE_SERVICE_ACCOUNT_FILE=/path/to/service-account-key.json
GOOGLE_DELEGATE_EMAIL=user@yourdomain.com

# Calendar to use (default: primary)
GOOGLE_CALENDAR_ID=primary

# ==================== BACKEND SERVER ====================
# Base URL for webhooks (use ngrok for local development)

BACKEND_BASE_URL=http://localhost:8000
# For production: BACKEND_BASE_URL=https://your-domain.com

# Secret for webhook signature verification
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=your-webhook-secret-here

# ==================== APPLICATION ====================

# Environment mode
NODE_ENV=development

# Timezone for appointments
TIMEZONE=America/New_York

# JWT Secret (for custom auth)
# Generate with: openssl rand -hex 64
JWT_SECRET=your-jwt-secret-here
JWT_EXPIRY_HOURS=24

# ==================== FRONTEND ====================
# Used by Vite (must be prefixed with VITE_)

VITE_API_BASE_URL=http://localhost:8000/api
